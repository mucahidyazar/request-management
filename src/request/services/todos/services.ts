import { ITodo } from '@/type';

import { api } from './api';
import { endpoints } from './endpoints';

export const createTodo = async () => {};

export const deleteTodo = async () => {};

export const detailTodo = async () => {};

export const listTodo = async () => {};

export const updateTodo = async () => {};
